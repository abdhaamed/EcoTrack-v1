'use client';

/* eslint-disable @typescript-eslint/no-unused-vars */

// ASSIGNED TO: Hamid
// CONTRACT: Client-side registration form with name/email/password validation
// IMPORTS (comment-only):
// import { Button } from '@/components/ui/Button'; (variants: primary, secondary, faint, outlined, ghost)
// import { Input } from '@/components/ui/Input'; (props: error?: boolean)
// import { Card } from '@/components/ui/Card'; (variants: bone, parchment)

interface RegisterFormProps {
  onSuccess?: () => void;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function RegisterForm({ onSuccess }: RegisterFormProps) {
  return null;
}
