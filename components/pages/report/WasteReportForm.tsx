'use client';

// ASSIGNED TO: Rifqi & Raja
// CONTRACT: Multi-step waste report submission form with photo upload and location picker
// IMPORTS (comment-only):
// import { Button } from '@/components/ui/Button'; (variants: primary, secondary, faint, outlined, ghost)
// import { Input } from '@/components/ui/Input'; (props: error?: boolean)
// import { Textarea } from '@/components/ui/Textarea'; (props: error?: boolean)
// import { Card } from '@/components/ui/Card'; (variants: bone, parchment)
// import { Spinner } from '@/components/ui/Spinner'; (props: size?: 'sm'|'md'|'lg')
// import { Toast } from '@/components/ui/Toast'; (props: variant?: 'default'|'error', children: React.ReactNode)

interface WasteReportFormProps {
  onSuccess?: () => void;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function WasteReportForm({ onSuccess }: WasteReportFormProps) {
  return null;
}
